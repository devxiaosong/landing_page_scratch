---
description: 所有 config 与 legal 内容不手填，由 AI 根据参考物料文档生成
globs: content/tools/**/*.config.ts, content/support/**/*.config.ts, content/legal/**/*.md, content/site.config.ts, docs/reference-material.md
alwaysApply: false
---

# 从参考物料生成配置与内容

**主要来源**：`docs/reference-material.md`。所有文案、FAQ、卖点、正文、品牌信息以此文档为基础，**不手填**。

## 覆盖范围

| 需要生成 / 更新的文件 | 参考物料对应节 | 类型 / 格式约束 |
|---|---|---|
| `content/tools/<id>.config.ts` | `## 工具：<名称>` | `ToolLandingConfig`（`lib/types.ts`） |
| `content/support/<id>.config.ts` | `## Support 页` | `SupportPageConfig`（`lib/types.ts`） |
| `content/legal/<slug>.md` | `## Legal Pages` 下对应子节 | Markdown + frontmatter（见下） |
| `content/site.config.ts` | `## Site 品牌信息` | `SiteConfig`（`lib/types.ts`） |

## AI 操作流程

1. 阅读 `docs/reference-material.md` 中**对应小节**（及「通用参考链接」节）。
2. 对照 `lib/types.ts` 类型定义；legal 页额外参考下方 frontmatter 格式。
3. 生成或覆盖对应文件，将参考物料整理为正式字段；外部参考链接只用于理解，**不直接写入输出文件**。
4. **内容创作策略**：用户只需提供工具名称、功能描述、目标用户、需要覆盖的关键词、参考链接等基础信息，**其余所有文案（主标题、一句话价值、Why/How 板块、功能点、使用步骤、用户评价、FAQ、CTA 等）由 AI 根据以上输入自主创作**，风格参照 `content/tools/` 下已有 config 的文案水准，**不停下来询问用户补充，直接生成**。
5. 生成文案时须将用户指定的**关键词自然融入**标题、描述、FAQ 等字段，确保 SEO 覆盖。

## legal 页 Markdown frontmatter 格式

```md
---
title: "Privacy Policy — {siteConfig.name}"   # SEO <title>
description: "..."                             # SEO <meta description>，≤160 字符
label: "Privacy Policy"                        # Footer 显示名称
order: 4                                       # Footer 排序，数字越小越靠前
---

正文（Markdown）...
```

## 排序约定（legal 页 order 参考值）

| slug | label | order |
|---|---|---|
| statement | Statement | 1 |
| license-agreement | License Agreement | 2 |
| terms-of-use | Terms of Use | 3 |
| privacy-policy | Privacy Policy | 4 |
| refund-policy | Refund Policy | 5 |
| cookies | Cookies | 6 |
| shipping-policy | Shipping Policy | 7 |
