import type { TestimonialsSection as TestimonialsSectionType } from "@/lib/types";

/**
 * 占位：此处插入用户评价板块 UI。
 * 数据已从 config 传入，重建 UI 时只改本组件内部，保留 props 不变。
 */
export default function TestimonialsSection(props: TestimonialsSectionType) {
  return (
    <section data-section="testimonials" aria-label="Testimonials">
      {/* 此处插入用户评价板块 — 数据: mainTitle, testimonials[] (quote, name, avatar) */}
    </section>
  );
}
