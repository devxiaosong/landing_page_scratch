import type { HeroSection } from "@/lib/types";

/**
 * 占位：此处插入 Hero 板块 UI。
 * 数据已从 config 传入，重建 UI 时只改本组件内部，保留 props 不变。
 */
export default function HeroBanner(props: HeroSection) {
  return (
    <section data-section="hero" aria-label="Hero">
      {/* 此处插入 Hero 板块 — 数据: title, description, windowsDownloadUrl, macDownloadUrl, productImage, ... */}
    </section>
  );
}
