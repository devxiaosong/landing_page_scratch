"use client";

/**
 * 占位：此处插入顶栏导航 UI。
 * 重建 UI 时只改本组件内部。如需多语言链接文案，可从 config 或 content 读入。
 */
export default function Header() {
  return (
    <header data-section="header" aria-label="Header">
      {/* 此处插入顶栏 — Logo、Guide、Support、Download 等导航 */}
    </header>
  );
}
