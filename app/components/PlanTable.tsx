import type { PlanTableSection } from "@/lib/types";

/**
 * 占位：此处插入价格/版本对比板块 UI。
 * 数据已从 config 传入，重建 UI 时只改本组件内部，保留 props 不变。
 */
export default function PlanTable(props: PlanTableSection) {
  return (
    <section data-section="plan-table" aria-label="Plan table">
      {/* 此处插入价格对比板块 — 数据: mainTitle, plans[], features[] */}
    </section>
  );
}
