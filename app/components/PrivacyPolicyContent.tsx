/**
 * 占位：此处插入隐私政策正文 UI。
 * 正文内容若已抽到 content/legal，则从 config 读入；否则重建 UI 时在本组件内实现，勿在占位内写长文案。
 */
export default function PrivacyPolicyContent() {
  return (
    <section data-section="privacy-policy-content" aria-label="Privacy policy content">
      {/* 此处插入隐私政策正文 */}
    </section>
  );
}
