import type { UserGuideSection as UserGuideSectionType } from "@/lib/types";

/**
 * 占位：此处插入使用步骤板块 UI。
 * 数据已从 config 传入，重建 UI 时只改本组件内部，保留 props 不变。
 */
export default function UserGuideSection(props: UserGuideSectionType) {
  return (
    <section data-section="user-guide" aria-label="User guide">
      {/* 此处插入使用步骤板块 — 数据: mainTitle, steps[], windowsDownloadUrl, macDownloadUrl */}
    </section>
  );
}
