"use client";

/**
 * 占位：此处插入页脚 UI。
 * 重建 UI 时只改本组件内部。如需多语言链接/文案，可从 config 或 content 读入。
 */
export default function Footer() {
  return (
    <footer data-section="footer" aria-label="Footer">
      {/* 此处插入页脚 — 链接、Support、法律页、版权等 */}
    </footer>
  );
}
