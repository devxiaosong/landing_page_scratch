import type { FAQSection as FAQSectionType } from "@/lib/types";

/**
 * 占位：此处插入 FAQ 板块 UI。
 * 数据已从 config 传入，重建 UI 时只改本组件内部，保留 props 不变。
 */
export default function FAQSection(props: FAQSectionType) {
  return (
    <section data-section="faq" aria-label="FAQ">
      {/* 此处插入 FAQ 板块 — 数据: mainTitle, faqs[] (question, answer) */}
    </section>
  );
}
