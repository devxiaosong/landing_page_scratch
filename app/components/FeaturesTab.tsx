import type { FeaturesSection } from "@/lib/types";

/**
 * 占位：此处插入功能展示板块 UI（Tab/轮播/宫格等）。
 * 数据已从 config 传入，重建 UI 时只改本组件内部，保留 props 不变。
 */
export default function FeaturesTab(props: FeaturesSection) {
  return (
    <section data-section="features" aria-label="Features">
      {/* 此处插入功能板块 — 数据: mainTitle, features[] (tabTitle, contentTitle, contentDescription, ...) */}
    </section>
  );
}
