/**
 * 占位：此处插入博客文章页顶部 Hero UI。
 * 重建 UI 时只改本组件内部。如需标题等可从文章数据传入 props。
 */
export default function ArticleHeroBanner() {
  return (
    <section data-section="article-hero-banner" aria-label="Article hero banner">
      {/* 此处插入文章页顶部 Hero */}
    </section>
  );
}
