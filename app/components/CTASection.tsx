import type { CTASection as CTASectionType } from "@/lib/types";

/**
 * 占位：此处插入底部 CTA 板块 UI。
 * 数据已从 config 传入，重建 UI 时只改本组件内部，保留 props 不变。
 */
export default function CTASection(props: CTASectionType) {
  return (
    <section data-section="cta" aria-label="CTA">
      {/* 此处插入底部 CTA 板块 — 数据: title, logo, windowsDownloadUrl, macDownloadUrl, ... */}
    </section>
  );
}
